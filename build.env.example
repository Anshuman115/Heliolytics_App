# Copy to build.env (gitignored) before personal release APK build.
# Values must match Heliolytics/deploy/.env on your server.

API_URL=https://your-api-host.example
API_SIGNING_SECRET=copy-from-deploy-env-HELIOLYTICS_SIGNING_SECRET
